# ===============================
# MySQL Database Connection
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3307/ems?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Chakri122006
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# Hibernate / JPA Config
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ===============================
# Server Config
# ===============================
# When deployed in Tomcat, context-path is handled by WAR packaging.
# If you run it directly with 'mvn spring-boot:run', it will use this:
server.port=9090
server.servlet.context-path=/ems-0.0.1-SNAPSHOT
